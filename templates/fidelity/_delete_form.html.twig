<form method="post" action="{{ path('app_fidelity_delete', {'id': fidelity.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ fidelity.id) }}">
    <button class="btn">Delete</button>
</form>
